"use strict";
function startVideo(videoMeta) {
    console.log(`startVideo ${videoMeta.videourl} , ${videoMeta.videoname}`);
}
const videoObj = {
    videourl: "url~~",
    videoname: "videoname"
};
startVideo(videoObj);
